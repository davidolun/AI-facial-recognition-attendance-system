# Build dependencies
setuptools>=65.0.0
wheel>=0.40.0

# Core Django
Django>=4.2.0,<5.0
gunicorn==21.2.0
whitenoise==6.6.0
python-decouple==3.8
dj-database-url==2.1.0

# Database
psycopg2-binary==2.9.7

# Skip image processing for now (causes build issues)
# Pillow==9.5.0
# numpy==1.24.3
# opencv-python-headless==4.8.1.78

# Skip face-recognition for now (has complex dependencies)
# face-recognition==1.3.0
# dlib==19.24.2